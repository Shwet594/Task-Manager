<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile - TaskManager</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          animation: {
            'fade-in': 'fadeIn 0.8s ease-in-out',
            'slide-up': 'slideUp 0.6s ease-out',
          }
        }
      }
    }
  </script>
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="bg-gray-900 text-white min-h-screen">

  <!-- Navigation -->
  <nav class="bg-gray-800 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center">
          <a href="/" class="text-2xl font-bold text-green-400">✅ TaskManager</a>
        </div>
        <div class="flex space-x-4">
          <a href="/" class="text-gray-300 hover:text-white px-3 py-2 rounded-lg transition duration-300">Home</a>
          <a href="/tasks" class="text-gray-300 hover:text-white px-3 py-2 rounded-lg transition duration-300">All Tasks</a>
          <a href="/user/logout" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded-lg transition duration-300 transform hover:scale-105">Logout</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="max-w-4xl mx-auto p-6 animate-fade-in">
    
    <!-- Profile Header -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg mb-8 animate-slide-up">
      <div class="flex items-center space-x-6">
        <div class="w-20 h-20 bg-gradient-to-r from-green-500 to-blue-500 rounded-full flex items-center justify-center text-white font-bold text-3xl">
          <%= user.name.charAt(0).toUpperCase() %>
        </div>
        <div>
          <h1 class="text-4xl font-bold bg-gradient-to-r from-green-400 via-blue-500 to-purple-500 bg-clip-text text-transparent mb-2">
            Hello, <%= user.name %>! 👋
          </h1>
          <p class="text-gray-400 text-lg">@<%= user.username %> • <%= user.tasks.length %> tasks</p>
        </div>
      </div>
    </div>

    <!-- Create Task Section -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg mb-8 animate-slide-up">
      <h2 class="text-2xl font-bold text-green-400 mb-6">🆕 Add New Task</h2>
      <form action="/tasks/create" method="post" class="space-y-6">
        <div>
          <label for="title" class="block text-sm font-semibold text-gray-300 mb-2">Task Title</label>
          <input 
            type="text"
            name="title"
            id="title"
            placeholder="Enter task title..."
            required
            class="w-full p-4 bg-gray-700 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 text-white placeholder-gray-400 transition duration-300"
          />
        </div>
        <div>
          <label for="description" class="block text-sm font-semibold text-gray-300 mb-2">Description</label>
          <textarea 
            name="description"
            id="description"
            placeholder="Describe your task..."
            required
            class="w-full h-24 p-4 bg-gray-700 border border-gray-600 rounded-lg resize-none focus:outline-none focus:ring-2 focus:ring-green-500 text-white placeholder-gray-400 transition duration-300"
          ></textarea>
        </div>
        <button 
          type="submit" 
          class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold px-8 py-3 rounded-lg transition duration-300 transform hover:scale-105 shadow-lg"
        >
          ➕ Add Task
        </button>
      </form>
    </div>

    <!-- User Tasks Section -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg animate-slide-up">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-2xl font-bold text-green-400">📋 Your Tasks</h2>
        <span class="text-gray-400 text-sm"><%= user.tasks.length %> total</span>
      </div>
      
      <% if(user.tasks && user.tasks.length > 0) { %>
        <div class="space-y-6">
          <% user.tasks.reverse().forEach((task) => { %>
            <div class="bg-gray-700 p-6 rounded-lg hover:bg-gray-600 transition duration-300 transform hover:scale-105">
              <!-- Task Header -->
              <div class="flex items-center justify-between mb-4">
                <div>
                  <h4 class="font-semibold text-white"><%= task.title %></h4>
                  <p class="text-gray-400 text-sm">ID: <%= task._id.toString().slice(-6) %></p>
                </div>
                <span class="text-sm px-3 py-1 rounded-full 
                  <%= task.status === 'completed' ? 'bg-green-600 text-white' 
                      : task.status === 'in-progress' ? 'bg-blue-500 text-white' 
                      : 'bg-yellow-500 text-black' %>">
                  <%= task.status.charAt(0).toUpperCase() + task.status.slice(1) %>
                </span>
              </div>
              
              <!-- Task Description -->
              <div class="text-gray-300 mb-4 leading-relaxed">
                <%= task.description %>
              </div>
              
              <!-- Task Actions -->
              <div class="flex items-center justify-between pt-4 border-t border-gray-600">
                <div class="flex items-center space-x-4">
                  <a 
                    href="/tasks/complete/<%= task._id %>" 
                    class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition duration-300 transform hover:scale-105 text-sm font-semibold"
                  >
                    ✅ Mark Complete
                  </a>
                </div>
                <div class="flex items-center space-x-4">
                  <a 
                    href="/tasks/edit/<%= task._id %>" 
                    class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg transition duration-300 transform hover:scale-105 text-sm font-semibold"
                  >
                    ✏️ Edit
                  </a>
                  <a 
                    href="/tasks/delete/<%= task._id %>" 
                    class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition duration-300 transform hover:scale-105 text-sm font-semibold"
                    onclick="return confirm('Are you sure you want to delete this task?')"
                  >
                    🗑️ Delete
                  </a>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <!-- Empty State -->
        <div class="text-center py-12">
          <div class="text-6xl mb-4 opacity-50">📭</div>
          <h3 class="text-xl font-semibold text-gray-400 mb-2">No tasks yet!</h3>
          <p class="text-gray-500 mb-6">Start by adding your first task.</p>
          <button 
            onclick="document.getElementById('title').focus()" 
            class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 px-6 py-3 rounded-lg font-semibold transition duration-300 transform hover:scale-105"
          >
            ➕ Add Your First Task
          </button>
        </div>
      <% } %>
    </div>

  </div>

</body>
</html>

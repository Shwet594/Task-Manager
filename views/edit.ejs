<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Task - TaskManager</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          animation: {
            'fade-in': 'fadeIn 0.8s ease-in-out',
            'slide-up': 'slideUp 0.6s ease-out',
          }
        }
      }
    }
  </script>
  <style>
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="bg-gray-900 text-white min-h-screen">

  <!-- Navigation -->
  <nav class="bg-gray-800 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center">
          <a href="/" class="text-2xl font-bold text-green-400">✅ TaskManager</a>
        </div>
        <div class="flex space-x-4">
          <a href="/" class="text-gray-300 hover:text-white px-3 py-2 rounded-lg transition duration-300">Home</a>
          <a href="/tasks" class="text-gray-300 hover:text-white px-3 py-2 rounded-lg transition duration-300">All Tasks</a>
          <a href="/logout" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded-lg transition duration-300 transform hover:scale-105">Logout</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="max-w-4xl mx-auto p-6 animate-fade-in">

    <!-- Header (matches profile header style) -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg mb-8 animate-slide-up">
      <div class="flex items-center space-x-6">
        <div class="w-20 h-20 bg-gradient-to-r from-green-500 to-blue-500 rounded-full flex items-center justify-center text-white font-bold text-3xl">
          <%= (user && user.name && user.name.charAt) ? user.name.charAt(0).toUpperCase() : 'U' %>
        </div>
        <div>
          <h1 class="text-4xl font-bold bg-gradient-to-r from-green-400 via-blue-500 to-purple-500 bg-clip-text text-transparent mb-2">
            Edit Task
          </h1>
          <p class="text-gray-400 text-lg">
            <%= user && user.username ? '@' + user.username : '' %>
            <% if (user && user.tasks) { %> • <%= user.tasks.length %> tasks<% } %>
          </p>
        </div>
      </div>
    </div>

    <!-- Edit Task Card (same card style as Create Task) -->
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg mb-8 animate-slide-up">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-2xl font-bold text-green-400">✏️ Edit Task</h2>
        <a href="/profile" class="text-sm text-gray-300 hover:text-white underline">← Back to Profile</a>
      </div>

      <form action="/tasks/update/<%= task._id %>" method="post" class="space-y-6">
        <!-- Hidden input to send task ID -->
        <input type="hidden" name="id" value="<%= task._id %>">

        <!-- Title -->
        <div>
          <label for="title" class="block text-sm font-semibold text-gray-300 mb-2">
            Task Title
          </label>
          <input 
            type="text"
            name="title"
            id="title"
            value="<%= task && task.title ? task.title : '' %>"
            required
            class="w-full p-4 bg-gray-700 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 text-white placeholder-gray-400 transition duration-300"
          />
        </div>

        <!-- Description / Content (supports either `description` or `Content`) -->
        <div>
          <label for="description" class="block text-sm font-semibold text-gray-300 mb-2">
            Description
          </label>
          <textarea 
            name="description"
            id="description"
            required
            class="w-full h-28 p-4 bg-gray-700 border border-gray-600 rounded-lg resize-none focus:outline-none focus:ring-2 focus:ring-green-500 text-white placeholder-gray-400 transition duration-300"
          ><%= (task && (task.description || task.Content)) ? (task.description || task.Content) : '' %></textarea>
          <!-- If your schema uses `Content` with capital C, your update controller should map req.body.description -> Content -->
        </div>

        <!-- Completed toggle (only if field exists) -->
        <% if (typeof task.completed !== 'undefined') { %>
          <div class="flex items-center gap-3">
            <input type="checkbox" id="completed" name="completed" value="true"
              class="h-5 w-5 rounded border-gray-600 bg-gray-700"
              <%= task.completed ? 'checked' : '' %> />
            <label for="completed" class="text-sm text-gray-300">Mark as completed</label>
          </div>
        <% } %>

        <!-- Actions -->
        <div class="flex items-center gap-4">
          <button 
            type="submit" 
            class="bg-gradient-to-r from-green-600 to-blue-600 hover:from-green-700 hover:to-blue-700 text-white font-semibold px-8 py-3 rounded-lg transition duration-300 transform hover:scale-105 shadow-lg"
          >
            ✅ Update Task
          </button>

          <a 
            href="/tasks/delete/<%= task._id %>" 
            class="bg-red-600 hover:bg-red-700 text-white font-semibold px-6 py-3 rounded-lg transition duration-300 transform hover:scale-105 shadow-lg"
            onclick="return confirm('Are you sure you want to delete this task?')"
          >
            🗑️ Delete
          </a>
        </div>
      </form>
    </div>

  </div>

</body>
</html>
